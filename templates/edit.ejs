<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit</title>
</head>
<body>
    
    <div>
        <h1>Edit link</h1>

        <form action="/edit/<%=body.id%>" method="POST">
            <input name="name" placeholder="Nome" value="<%=body.name%>" required>
            <input name="category"  placeholder="Categorio" value="<%=body.category%>" required>
            <input name="description" placeholder="Descrição" value="<%=body.description%>" required>
            <input name="urlImage" placeholder="URL da imagem" value="<%=body.urlImage%>" required>
            <input name="urlVideo" placeholder="URL do Video" value="<%=body.urlVideo%>" required>
            <button>Salvar</button>

        </form>
    </div>
</body>
</html>